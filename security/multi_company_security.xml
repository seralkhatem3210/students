<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
    
        <record model="ir.rule" id="discounts">
            <field name="name">Discounts </field>
            <field name="model_id" ref="model_discounts"/>
            <field name="global" eval="True"/>
            <!-- <field name="domain_force">['|',('company_id','=',False),('company_id','in', company_ids)]</field> -->
        </record>

        <record model="ir.rule" id="student_genderr">
            <field name="name">Gender</field>
            <field name="model_id" ref="model_gender"/>
            <field name="global" eval="True"/>
        </record>

    </data>
</odoo>
